
## 数据结构
书面陈述
```
个人观点> 
	无非三个重要的数据结构，请求、 对象 和磁盘.
基本问题：
	1. 处理同一时间的同一个对象的多个请求.
	2. 处理 对象与请求之间的联系，包括但不限于：读入请求时，针对对象的处理；删除对象时，针对请求的处理；读取完对象时，针对请求的处理；
	3. 对象自身的操作，包括但不限于：写入对象时，处理对象和磁盘的关系；对象块的处理操作等等...
	4. 磁盘自身的操作，包括但不限于：磁盘的读写操作.
	
	显然，上述问题暂时与优化问题无关.
请求数据结构：
	我认为，对象请求应该细化为对象块的请求，即 对于一个 r_id， 要求将其划分为：r_id_1, r_id_2 (假设该对象的大小 size == 2).
	此外，定义管理对象块的类blockManager(此后简称bM)，每一个对象均有一个 bM，且同一个bM仅属于一个对象.在每一个bM内部，有 size 个数组，用以管理对象块，即 bl[i] 记录哪些请求r_id 需要第i个对象块，例如 bl[2] = {3, 5} 表示 请求3和请求5依旧需要第2个对象块.
	案例：
	1.当我们在某一时间片 在某一个磁盘上读取了对象的第2个对象块，那么 我们需要找到该对象对应的 bM查询对应的 bl[2]，假如 bl[2] = {3}, 那么我们应该告诉请求3，已读取到了第2个对象块。倘若此时请求3已收集到所有的对象块，那么将请求3上报，并清空 bl[2].
    2.当我们在某一个时间片读取请求r_id时，假如该对象请求具有两个对象块，那么找到对应的bM，r_id 同时插入bl[1].push_back(r_id), bl[2].push_back(r_id).
    3. 更加详细的解释如下：
    在时间片1 读入大小size = 2的对象请求r_id1，通知 bl插入r_id1，此刻 bl[1] = {r_id1}, bl[2] = {r_id1}；
    在时间片2，读取到某个磁盘的第一个对象块，那么 清空bl[1]吗，此刻 bl[1] = { }, bl[2] = {r_id1}；
    在时间片3，读入相同对象的请求r_id2，通知 bl 插入r_id2，此刻 bl[1] = {r_id2}, bl[2] = {r_id1, r_id2}；
    在时间片4，读取到某个磁盘的第二个对象块，那么 清空bl[2]，此刻 bl[1] = {r_id2}, bl[2] = {}.
    
    当我们接触到删除操作时，首先要处理没有完成的请求，查询此类请求很简单，遍历每一个 bl[i]，假如 r_id 存在于 bl[i]， 那么说明r_id没有被处理完成. 遍历完成时，不妨清空你每一个 bl[i].
    
    待续……
```